"""
Stub for distributed training runner.
"""


def run_distributed_training(agent_cls, env_fn, n_workers=4):
    # Placeholder: spawn workers, share parameters, aggregate results
    print(f"Running distributed training with {n_workers} workers...")
